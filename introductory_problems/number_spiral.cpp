#include <bits/stdc++.h>

using namespace std;

int main() {
    cin.tie(nullptr)->sync_with_stdio(false);

    int64_t t, y, x;
    cin >> t;
    for (int64_t i=1; i<t; ++i) {
        cin >> y >> x;

    }
    cout << t << endl;

    return EXIT_SUCCESS;
}